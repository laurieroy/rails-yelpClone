<p id="notice"><%= notice %></p>
<div class="card bg-light px-4 py-5 my-5 text-center">

  <div class="d-grid gap-2 d-sm-flex justify-content-sm-end">
<%#= TODO: add in backsplash of user submitted photos, cover 
Configure search %>
    <button type="button" class="btn btn-outline-white btn-lg px-4 gap-3">Log In</button>
    <button type="button" class="btn btn-outline-secondary btn-lg px-4">Sign Up</button>
  </div>
  <h1 class="display-5 fw-bold">YelpClone is the best way to find reviews on local restaurants.</h1>
  <h3>Explore the favorite diners, bakeries and food trucks in your community.</h3>
  <br >

  <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Leave reviews and ratings for your fellow foodies...</p>
              <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
  
      </div>
  </div>
<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= restaurant.name %></td>
        <td><%= restaurant.address %></td>
        <td><%= restaurant.phone %></td>
        <td><%= link_to restaurant.website, restaurant.website %></td>
        <td><%= link_to 'Show', restaurant, class: "btn btn-link"  %></td>
        <td><%= link_to 'Edit', edit_restaurant_path(restaurant),  class: "btn btn-link"  %></td>
        <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' },  class: "btn btn-link"  %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Restaurant', new_restaurant_path, class: "btn btn-link"  %>
